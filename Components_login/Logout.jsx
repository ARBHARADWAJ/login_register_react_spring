import React, { useContext } from "react";
import { Sharedstate } from "../UsageStateElements";
import { Button } from "bootstrap";

export const Logout = ({ onLogout }) => {
  // const {handleLogout} = useContext(Sharedstate);


  return (
    <>
      
    </>
  );
};
